# SoftwareRasteriser

From inside the build directory, in the x64 Native Tools Command Prompt

cmake -G "Visual Studio 18 2026" ../source

cmake --build . --config Release

"Release/main.exe"
